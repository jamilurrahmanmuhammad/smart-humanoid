# Smart Humanoid Platform - Environment Configuration
# Copy this file to .env.local and configure as needed

# ===========================================
# Feature Flags (all disabled by default)
# ===========================================

# Authentication - Enable for user login/signup
ENABLE_AUTH=false

# LLM Integration - Enable for AI-powered features
ENABLE_LLM=false

# Vector Search - Enable for RAG/semantic search
ENABLE_VECTOR_SEARCH=false

# Analytics - Enable for usage tracking
ENABLE_ANALYTICS=false

# ===========================================
# Provider Configuration (set when enabling features)
# ===========================================

# Authentication Provider (when ENABLE_AUTH=true)
# AUTH_PROVIDER=better-auth
# AUTH_SECRET=your-secret-here

# LLM Provider (when ENABLE_LLM=true)
# LLM_PROVIDER=openai
# LLM_API_URL=https://api.openai.com/v1
# LLM_API_KEY=your-api-key-here
# LLM_MODEL=gpt-4

# Vector Database (when ENABLE_VECTOR_SEARCH=true)
# VECTOR_PROVIDER=qdrant
# VECTOR_API_URL=https://your-qdrant-instance.com
# VECTOR_API_KEY=your-api-key-here

# Analytics (when ENABLE_ANALYTICS=true)
# ANALYTICS_PROVIDER=plausible
# ANALYTICS_DOMAIN=your-domain.com

# ===========================================
# CI Environment Detection
# ===========================================
# The CI variable is automatically set to "true" by all major CI/CD platforms:
# - GitHub Actions, GitLab CI, CircleCI, Travis CI, Jenkins
# - Vercel, Netlify, Cloudflare Pages, Render
#
# You do NOT need to set this manually. The build system detects it automatically.
# CI=true

# ===========================================
# Docusaurus Site Deployment (SITE_URL, BASE_URL)
# ===========================================
# These configure where your documentation site will be deployed.
# Defaults work for local development - no changes needed for `npm start`.
#
# ZERO-CONFIG DEPLOYMENT:
# Most hosting platforms (Vercel, Netlify, Cloudflare Pages) will automatically
# detect and configure these. You only need to set them for:
# - Custom domains
# - Subdirectory deployments (e.g., GitHub Pages project sites)

# Full canonical URL where the site will be hosted
# Default: http://localhost:3000 (local development)
# Examples: https://docs.example.com, https://company.github.io
SITE_URL=http://localhost:3000

# Base URL path prefix for the site
# Default: / (root of domain)
# Must start with /
# Examples: /, /docs/, /smart-humanoid/
BASE_URL=/

# GitHub Pages example (uncomment for production):
# SITE_URL=https://jamilurrahmanmuhammad.github.io
# BASE_URL=/smart-humanoid/
